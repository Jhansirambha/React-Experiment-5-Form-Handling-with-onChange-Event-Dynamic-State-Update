# React-Experiment-5-Form-Handling-with-onChange-Event-Dynamic-State-Update
Create a **React form** that dynamically updates state values as the user types, demonstrating real-time two-way data binding using the **onChange event** and **useState hook**.
